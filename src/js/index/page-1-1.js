{
    let view = {
        el: 'section.playlists'
    }
    let model = {}
    let controller = {
        init() {
            view,
            model
        }
    }
    controller.init(view, model)
}